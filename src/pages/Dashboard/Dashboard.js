import React from 'react';
import PropTypes from 'prop-types';

import './Dashboard.css';

const Dashboard = () => {
    return (
        <div>
            Dashboard
        </div>
    );
};

Dashboard.propTypes = {};

export default Dashboard;